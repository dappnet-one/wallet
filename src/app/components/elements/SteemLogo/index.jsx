import React from 'react';
import PropTypes from 'prop-types';

const DappnetLogo = () => {
    return (
        <span className="logo">
            <b>DappNet</b>
        </span>
    );
};

export default DappnetLogo;
